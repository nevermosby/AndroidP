<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liwenq.loveubymoment" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ViewPagerContentActivity"
            android:label="@string/title_activity_listview" >
        </activity>
        <activity
            android:name=".ViewPagerActivity"
            android:label="@string/title_activity_view_pager" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DayDetailActivity"
            android:label="@string/title_activity_day_detail" >
        </activity>
        <activity
            android:name=".TabViewActivity"
            android:label="@string/title_activity_tab_view" >
        </activity>

        <receiver android:name=".Service.AlarmManagerBroadcastReceiver" android:enabled="true">
        </receiver>
        <service android:name=".Service.MemorialDayCheckService" />
    </application>

</manifest>
